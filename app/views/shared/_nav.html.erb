<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        Speakeasy
      </a>
    </div>
      <nav class="nav navbar-nav navbar-right">
        <ul class="nav navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              <%= user_signed_in? ? "#{current_user.name}" : "Guest" %> 
              <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <% if user_signed_in? %>
                <li>You are signed in.</li>
                <li role="separator" class="divider"></li>
                <li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: "navbar-link" %></li>
              <% else %>
                  <%= content_tag :li, link_to('Sign Up', new_user_registration_path, class: "navbar-link") if controller_name != 'registrations' %>
                  <%= content_tag :li, link_to('Log in', new_user_session_path, class: "navbar-link") if controller_name != 'sessions' %>
              <% end %>
            </ul>
            <%# content_tag :li, switch_user_select(class: "form-control"), style: "position: relative; top: 7px; margin-right: 10px;" if Rails.env.development? %>
          </li>

        </ul>
      </nav>
    <!-- </div> -->
  </div>
</nav>